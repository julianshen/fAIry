{
  "name": "fairy",
  "version": "0.0.0",
  "private": true,
  "description": "A browser agent you talk to — Chrome extension + local Pi agent daemon.",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "bun": ">=1.3.0"
  }
}
